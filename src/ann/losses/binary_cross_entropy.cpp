/**
 * @file binary_cross_entropy.cpp
 * @brief 
 * @author Jan Warchocki
 * @date 24 December 2022
 */

#include <loss.h>

float BinaryCrossEntropy::calculateLoss(const Tensor& targets, const Tensor& predictions) {
    return 0;
}

void BinaryCrossEntropy::calculateDerivatives(const Tensor& targets, const Tensor& predictions,
                                              Tensor& destination) const {

}